#include "global.h"
#include "proc.h"
#include "event.h"

#include "connector_config.h"
#include "archipelago.h"

void markSethRecruited(struct EventEngineProc *proc) {
  handleUnitRecruited(proc, Seth);
}

void markFranzRecruited(struct EventEngineProc *proc) {
  handleUnitRecruited(proc, Franz);
}

void markGilliamRecruited(struct EventEngineProc *proc) {
  handleUnitRecruited(proc, Gilliam);
}

void markVanessaRecruited(struct EventEngineProc *proc) {
  handleUnitRecruited(proc, Vanessa);
}

void markMoulderRecruited(struct EventEngineProc *proc) {
  handleUnitRecruited(proc, Moulder);
}

void markRossRecruited(struct EventEngineProc *proc) {
  handleUnitRecruited(proc, Ross);
}

void markGarciaRecruited(struct EventEngineProc *proc) {
  handleUnitRecruited(proc, Garcia);
}

void markNeimiRecruited(struct EventEngineProc *proc) {
  handleUnitRecruited(proc, Neimi);
}

void markColmRecruited(struct EventEngineProc *proc) {
  handleUnitRecruited(proc, Colm);
}

void markArturRecruited(struct EventEngineProc *proc) {
  handleUnitRecruited(proc, Artur);
}

void markLuteRecruited(struct EventEngineProc *proc) {
  handleUnitRecruited(proc, Lute);
}

void markNatashaRecruited(struct EventEngineProc *proc) {
  handleUnitRecruited(proc, Natasha);
}

void markJoshuaRecruited(struct EventEngineProc *proc) {
  handleUnitRecruited(proc, Joshua);
}

void markFordeRecruited(struct EventEngineProc *proc) {
  handleUnitRecruited(proc, Forde);
}

void markKyleRecruited(struct EventEngineProc *proc) {
  handleUnitRecruited(proc, Kyle);
}

void markTanaRecruited(struct EventEngineProc *proc) {
  handleUnitRecruited(proc, Tana);
}

void markAmeliaRecruited(struct EventEngineProc *proc) {
  handleUnitRecruited(proc, Amelia);
}

void markInnesRecruited(struct EventEngineProc *proc) {
  handleUnitRecruited(proc, Innes);
}

void markGerikRecruited(struct EventEngineProc *proc) {
  handleUnitRecruited(proc, Gerik);
}

void markTethysRecruited(struct EventEngineProc *proc) {
  handleUnitRecruited(proc, Tethys);
}

void markMarisaRecruited(struct EventEngineProc *proc) {
  handleUnitRecruited(proc, Marisa);
}

void markLArachelRecruited(struct EventEngineProc *proc) {
  handleUnitRecruited(proc, LArachel);
}

void markDozlaRecruited(struct EventEngineProc *proc) {
  handleUnitRecruited(proc, Dozla);
}

void markSalehRecruited(struct EventEngineProc *proc) {
  handleUnitRecruited(proc, Saleh);
}

void markEwanRecruited(struct EventEngineProc *proc) {
  handleUnitRecruited(proc, Ewan);
}

void markCormagRecruited(struct EventEngineProc *proc) {
  handleUnitRecruited(proc, Cormag);
}

void markRennacRecruited(struct EventEngineProc *proc) {
  handleUnitRecruited(proc, Rennac);
}

void markDuesselRecruited(struct EventEngineProc *proc) {
  handleUnitRecruited(proc, Duessel);
}

void markKnollRecruited(struct EventEngineProc *proc) {
  handleUnitRecruited(proc, Knoll);
}

void markMyrrhRecruited(struct EventEngineProc *proc) {
  handleUnitRecruited(proc, Myrrh);
}

void markSyreneRecruited(struct EventEngineProc *proc) {
  handleUnitRecruited(proc, Syrene);
}
